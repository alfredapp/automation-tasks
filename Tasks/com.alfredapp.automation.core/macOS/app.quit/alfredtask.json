{
  "configurableConfig" : [
    {
      "config" : {
        "default" : "",
        "placeholder" : "e.g. Messages, Safari",
        "required" : false,
        "trim" : true
      },
      "description" : "Comma-separated list of app names, paths, or bundle identifiers. Add apps to the list on-demand by passing them as arguments: take advantage of the \"Arg and Vars\" and \"Split Arg\" Utilities.",
      "label" : "Quit",
      "type" : "textfield",
      "variable" : "app_list"
    }
  ],
  "inputMulti" : -1,
  "inputType" : 0,
  "outputType" : 0,
  "script" : "\/\/ TODO: Needs 0+ Input\n\/\/ TODO: Testing\n\nObjC.import(\"stdlib\")\n\nfunction run(argv) {\n  const fromVar = $.getenv(\"app_list\")\n    .split(\",\")\n    .map(app => app.trim())\n    .filter(app => app.length > 0)\n\n  const apps = argv.concat(fromVar)\n\n  apps.forEach(app => Application(app).quit())\n}",
  "scriptType" : 7,
  "taskDescription" : "Quits passed-in and configured apps",
  "taskName" : "Quit Apps"
}